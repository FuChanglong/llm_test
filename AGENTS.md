# llm_test Engineering Rules

本项目的目标不只是“功能可用”，还要持续控制复杂度。任何改动都应优先选择最小、最清晰、最可维护的落点，避免继续把系统写重。

## 工作方式

1. 先读现有结构，再动手；优先复用已有模块、类型、接口和模式。
2. 能通过小改动接入现有结构，就不要新建体系、层级或通用抽象。
3. 每次实现前先判断：这次改动是否会让项目更重、更散、更难删；如果会，先收敛方案。
4. 改动必须可删、可测、可回退，边界清晰。
5. 默认用中文回答，除非用户明确使用英文提问。

## 当前代码结构风险

以下文件已经明显过载，新增功能默认不要继续堆进去，除非只是很小的展示修正或局部 bugfix：

- `api_server.py`
- `frontend/src/App.tsx`

如果需求需要继续修改这些文件，先评估是否应做最小拆分，再接功能。

## 后端规则

1. 路由层只做四件事：收参、鉴权、调用业务函数、返回响应。
2. 业务逻辑下沉到领域模块；不要把状态流转、分支判断、组合流程直接写在路由里。
3. 数据存取集中在 store/repository 层；不要在业务逻辑里散落文件系统、数据库、持久化细节。
4. 请求体、响应体、DTO、领域类型集中管理，避免重复定义结构相近的字典或模型。
5. 配置、常量、环境变量、错误码集中管理，禁止散落 magic string 和 magic number。
6. 外部服务调用、文件处理、索引更新、格式转换不要直接堆进路由。
7. 异常处理要统一，明确区分：
   - 用户输入错误
   - 未认证/无权限
   - 资源不存在
   - 资源冲突
   - 外部依赖失败
   - 内部错误

### 后端拆分优先级

新增后端功能时，优先按以下领域落点，而不是继续扩大单文件职责：

- `auth`
- `workspace`
- `session/chat`
- `rag/documents`
- `memory`
- `research`
- `images`

## 前端规则

1. 页面组件负责状态编排和视图组合，不直接承载大段请求逻辑和复杂数据转换。
2. API 请求统一放在 `frontend/src/api/` 下，组件内不要散落 `fetch`/`axios`。
3. 组件按职责拆分：
   - 页面容器：数据流和状态
   - 展示组件：渲染
   - 交互组件：局部行为
4. 不要继续往 `App.tsx` 堆积状态；状态过多时按业务域拆分。
5. 复杂判断、响应结构适配、数据映射不要直接塞进 JSX。
6. 类型统一管理，避免组件内部临时拼结构。
7. 样式延续现有项目风格，不要因为单个功能引入新的视觉体系。

## 文件组织

1. 新增代码前先判断是否能放入现有合适模块；只有现有文件职责过载时才新建文件。
2. 新文件必须职责明确，命名体现业务域；禁止 `helpers.ts`、`common2.ts`、`temp_utils.py` 这类模糊命名。
3. 目录优先按业务领域组织，而不是按“杂项工具”堆放。
4. 同一领域的类型、API、业务逻辑、组件尽量相邻。

## 并发、一致性与原子性

只要涉及并发、异步任务、多请求共享状态、文件写入、知识库更新、索引重建，都必须先考虑一致性和恢复路径。

1. 先识别共享资源、竞争点和读写顺序，不允许默认假设“不会并发发生”。
2. 需要明确使用何种保护策略：锁、事务、串行化、任务队列、版本号、幂等键或原子替换。
3. 文件写入尽量原子化：
   - 先写临时文件
   - 校验完成
   - 再 `rename`/`replace`
   - 避免直接覆盖正式文件
4. 任何缓存、导出、索引、元数据写入都要避免半完成状态被读取。
5. 知识库、RAG、向量索引、附件、会话元数据更新必须保证一致性：
   - 明确写入顺序
   - 明确失败回滚或补偿策略
   - 不允许“记录已存在但索引未完成”或“索引已更新但记录未同步”
6. 重建、删除、迁移、批量导入优先采用“新副本构建完成后再切换”的策略，避免破坏在线读取。
7. 所有可能重试的操作优先设计为幂等。

## 安全与稳定性

1. 所有输入都要做边界校验，不信任前端输入、文件名、路径和第三方返回值。
2. 涉及上传、导出、删除、预览时，要防止路径穿越、非法覆盖、脏文件残留。
3. 不把敏感信息写进日志、异常文本或前端响应。
4. 外部依赖必须考虑超时、重试、失败降级或错误隔离。
5. 对已有接口、数据格式和前端调用链保持兼容；如果必须调整，说明影响范围和迁移方式。

## 测试要求

1. 只补与当前改动直接相关的测试，不做泛滥补测。
2. 高风险改动优先补：
   - 并发
   - 鉴权
   - 文件写入
   - 索引/知识库更新
   - 状态流转
3. 除了成功路径，还要验证失败路径、重复执行、空输入和边界输入。
4. 如果某部分无法充分测试，要明确说明残余风险。

## 输出要求

每次实现代码时，按这个顺序输出：

1. 先简述本次改动的落点和分层方案。
2. 再实现代码。
3. 最后说明修改了哪些文件，以及每个文件承担什么职责。
4. 如果涉及并发、文件写入、知识库更新，必须说明安全性、一致性和原子性处理。

## 禁止事项

1. 不要继续把新功能直接堆进超长的 `App.tsx` 或 `api_server.py`。
2. 不要复制请求逻辑、业务规则或数据转换逻辑。
3. 不要写“什么都干”的函数。
4. 不要为了抽象而制造大量空壳层。
5. 不要引入没有明确收益的新框架、新状态管理或新目录体系。
6. 不要留下“先这样能跑，后面再整理”的结构性债务，除非用户明确接受该取舍。
